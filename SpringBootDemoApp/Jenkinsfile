node{
  stage('SCM Checkout'){
    git 'https://github.com/kaymonCodeM/SpringBootPractice.git'
  }
  stage('Compile-Package'){
    // Get maven home path
    def mvnHome = tool name: 'maven-30', type: 'maven'
    sh "${mvnHome}/bin/mvn package"
  }
}
